#table-placeholder
-# %table#tbl.table.stripe.display{"data-toggle" => "table"}
  -# %thead
  -#   - @titles.each do |col|
  -#     %th{"data-field" => col} =col
  -# %tfoot
  -#   - @titles.each do |col|
  -#     %th =col

:javascript
  $.get( "index.json", function( data ) {
      drawTable( data, function() {
        alert('in callback');
        var table = $('#tbl').DataTable();
        table.column( 0 ).visible( false );
      });
    });
